FROM mariadb:10.11

ENV MARIADB_ROOT_PASSWORD=${MARIADB_ROOT_PASSWORD:-XXXX}
ENV MARIADB_DATABASE=agro_monitoring
ENV MARIADB_USER=${MARIADB_USER:-XXXX}
ENV MARIADB_PASSWORD=${MARIADB_PASSWORD:-XXXX}

COPY init.sql /docker-entrypoint-initdb.d/

VOLUME ["/var/lib/mysql"]

EXPOSE 3306
